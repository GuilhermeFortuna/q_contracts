{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "api/error",
  "title": "ApiError",
  "description": "Standard error response shape across the control surface.",
  "type": "object",
  "required": [
    "message"
  ],
  "properties": {
    "message": {
      "type": "string",
      "description": "Human-readable description of the error."
    },
    "code": {
      "type": "string",
      "description": "Machine-readable error code classifying the error condition (e.g. idempotency_key_required, idempotency_key_reused, idempotency_in_progress)."
    },
    "details": {
      "description": "Optional structured error details or contextual metadata."
    }
  },
  "additionalProperties": false
}
